/*----------------------------------------------------------------
A program that changes the value of a float through the use of a 
pointer. Fill in the TODO sections below.
----------------------------------------------------------------*/

#include <stdio.h>

int main(){
	
    float x = 2.5;
    printf("x = %f\n", x);

    /* TODO: Define a pointer here and assign it the value of the 
             address of x. 
    */

    /* TODO: Using the pointer, change the value of x to 5.5
    */

    printf("After changing the value of x with a pointer...\n");
    printf("x = %f\n", x);

    return 0;
}
